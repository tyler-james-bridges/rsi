# RSI starts with every external adapter disabled.
# Add provider configuration only when implementing that adapter.
# Never place wallet private keys or recovery material in this file.

RSI_LOG_LEVEL=info
RSI_STORAGE_PATH=.local/rsi.db
