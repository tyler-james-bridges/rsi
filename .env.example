# RSI currently requires no environment configuration. Every external adapter
# is disabled. Add only documented, adapter-specific variables when a read-only
# integration is implemented; never place wallet keys or recovery material here.
